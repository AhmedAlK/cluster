#include <stdio.h>
#include <stdlib.h>
#include "cluster.h"


int process(BUFFER buffer){
	//split buffer up

	//write to socket

	return EXIT_SUCCESS;
}

void create_buffer(unsigned char *static_data, unsigned char *parallel_data){
	// Take static data, tape it to a 
}
